name: simple first action
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: print something
        run: echo "Hello World"
      - name: say goodbye
        run: echo "bye bye!!!"
